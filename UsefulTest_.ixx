export module UsefulTest;
import std;

export namespace UsefulTestNS {
	/*template<typename T>
	std::vector<T> createVector(T args...) {
		std::vector<T> v{};
		for (T arg : args) {
			v.push_back(arg);
		}
		return v;
	}

	template<typename T>
	std::vector<T> createVector() {
		return {};
	}*/
}